import React from 'react';

const Assignment2 = () => {
    return (
        <p>
            {/*
                After adding ExpensesFilter.js and ExpensesFilter.css files we have to add a listener
                to the select element. We can add the onChange prop and do the other operations as we
                have learned in the course so far.

                Bonus:
                To achieve two way binding for the dropdown. To achieve that we can pass in the
                filteredYear back into the ExpensesFilter to set up the two way binding.
            */}
        </p>
    )
}

export default Assignment2;