import React from 'react';

const AnAlternativeFunctionSyntax = () => {
    return (
        <p>
            {/*
                We can also write an Arrow function instead of a regular function. Though it is an
                optional thing and there is no benefit of using an Arrow function instead of a regular
                function.
            */}
        </p>
    )
}

export default AnAlternativeFunctionSyntax;